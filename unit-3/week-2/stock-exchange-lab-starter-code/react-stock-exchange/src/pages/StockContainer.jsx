import React from 'react';
import StockCard from '../components/StockCard'

export default function StockContainer() {
    return (
        <div>
        <h2>Stocks</h2>
        { /* render the list of stocks here using the StockCard component, pass the stocks prop */ }
        </div>
    );
}